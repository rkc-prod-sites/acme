# Local scratch / source files not used by the site
New folder/
/logo.png

# Local dev-preview tooling (not used by the deployed site)
.claude/

# Project notes, not part of the deployed site
/README.md

# OS junk
Thumbs.db
.DS_Store

# Node (if ever used for local tooling)
node_modules/
package-lock.json
